services:

  app:
    depends_on:
      redis:
        condition: service_started

  redis:
    image: redis:7-alpine
    container_name: ez-php-view-cache-redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
